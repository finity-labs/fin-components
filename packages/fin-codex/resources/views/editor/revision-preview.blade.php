{{-- The revision slide-over's body: the rendered revision, title first, with the core lightbox, under its own marker. --}}
@include('fin-codex::editor.rendered-article', ['marker' => 'data-fin-codex-revision-preview'])
